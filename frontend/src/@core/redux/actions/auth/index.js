// ** UseJWT import to get config

// ** Handle User Login
export const handleLogin = (data) => {
  return (dispatch) => {}
}

// ** Handle User Logout
export const handleLogout = () => {
  return (dispatch) => {}
}
